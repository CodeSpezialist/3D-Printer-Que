services:
  octoprint:
    container_name: octoprint
    image: octoprint/octoprint
    ports:
      - "1234:80"
    volumes:
      - octoprint:/octoprint
    restart: always
    networks:
      - printer-que

volumes:
  octoprint:

networks:
  printer-que: